export enum channel {
	menu = 'menu',
}

export enum menuOption {
	connect = 'connect',
	disconnect = 'disconnect',
	runScript = 'runScript',
	runStatement = 'runStatement',
	commit = 'commit',
	rollback = 'rollback',
	closeAllTabs = 'closeAllTabs',
	export = 'export',
}

