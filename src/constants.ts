export enum channel {
	menu = 'menu',
}

export enum menuOption {
	connect = 'connect',
	disconnect = 'disconnect',
	run = 'run',
	commit = 'commit',
	rollback = 'rollback',
	closeAllTabs = 'closeAllTabs',
	export = 'export',
}

